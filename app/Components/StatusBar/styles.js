import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: '15%',
        alignItems: 'center',
        flexDirection: 'column',
        flex: 1
    }
})